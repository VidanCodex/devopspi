pipeline {
    agent any

    parameters {
        string(name: 'DOCKER_TAG', defaultValue: 'latest', description: 'Docker image tag')
    }

    environment {
        SERVICE_NAME   = 'api-gateway'
        DOCKER_IMAGE   = 'vidancodex/pi2-api-gateway'
        NAMESPACE      = 'pi-soyosa'
        DEPLOYMENT     = 'api-gateway-deployment'
        KUBECONFIG     = "/var/lib/jenkins/.kube/config"
        TAG            = "${params.DOCKER_TAG ?: 'latest'}"
        EMAIL_CREDS    = credentials('gmail-app-password')
    }

    stages {

        stage('Setup Monitoring') {
            steps {
                sh '''
                    kubectl apply -f monitoring/namespace.yaml
                    kubectl apply -f monitoring/prometheus-config.yaml
                    kubectl apply -f monitoring/prometheus-deployment.yaml
                    kubectl apply -f monitoring/grafana-deployment.yaml
                '''
            }
        }

        stage('Check Kubernetes') {
            steps {
                sh '''
                    kubectl cluster-info
                    kubectl get nodes
                '''
            }
        }

        stage('Pull Docker Image') {
            steps {
                sh "docker pull ${DOCKER_IMAGE}:${TAG}"
            }
        }

        stage('Deploy to Kubernetes') {
            steps {
                dir('api-gateway') {

                    sh '''
                        kubectl apply -f api-gateway-deployment.yaml
                        kubectl apply -f api-gateway-service.yaml
                    '''

                    sh """
                        kubectl set image deployment/${DEPLOYMENT} \
                        api-gateway=${DOCKER_IMAGE}:${TAG} \
                        -n ${NAMESPACE}
                    """

                    sh '''
                        kubectl rollout status deployment/${DEPLOYMENT} \
                        -n ${NAMESPACE} --timeout=120s
                    '''
                }
            }
        }

        stage('Health Check') {
            steps {
                sh '''
                    kubectl get pods -n ${NAMESPACE}
                    kubectl get svc -n ${NAMESPACE}
                '''
            }
        }
    }

    post {

        success {
            emailext(
                subject: "Build SUCCESS: ${env.JOB_NAME} #${env.BUILD_NUMBER}",
                body: "The API Gateway deployment completed successfully.\nCheck details: ${env.BUILD_URL}",
                to: "jomniyacine@gmail.com"
            )

            echo 'CD pipeline completed successfully!'
        }

        failure {
            emailext(
                subject: "Build FAILED: ${env.JOB_NAME} #${env.BUILD_NUMBER}",
                body: "The API Gateway deployment failed.\nCheck logs: ${env.BUILD_URL}",
                to: "jomniyacine@gmail.com"
            )

            echo 'Deployment failed.'
        }
    }
}